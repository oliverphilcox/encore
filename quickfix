for i in {1..250}
  do
  echo Using simulation $i
  mkdir tmp
  cp patchy${i}_cmass.tgz tmp/
  cd tmp
  tar -xvf patchy${i}_cmass.tgz
  python ~/NPCF-Estimator/python/combine_files.py patchy${i}_cmass 16
  cp patchy${i}_cmass.zeta_4pcf.txt ../
  cd ../
  rm -rf tmp
done
